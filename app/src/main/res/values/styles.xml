<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Base.Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="MenuAvatarStyle">
        <item name="android:padding">5dp</item>
        <item name="android:layout_marginLeft">5dp</item>
    </style>

    <style name="ButtonOutlineAccent"><!--ABSTRACT-->
        <item name="android:textAlignment">center</item>
        <item name="android:textAllCaps">true</item>
        <item name="android:textColor">@color/greyscale_12</item>
        <item name="android:textSize">13sp</item>
    </style>
    <style name="OvalButtonOutlineAccent">
        <item name="android:background">@drawable/oval_outline_accent</item>
    </style>
    <style name="ThickOvalButtonOutlineAccent" parent="ButtonOutlineAccent">
        <item name="android:background">@drawable/thick_oval_outline_accent</item>
    </style>

    <style name="NewPhotoButton">
        <item name="android:background">@drawable/rounded_outline_accent</item>
        <item name="android:layout_marginBottom">25dp</item>
        <item name="android:paddingTop">10dp</item>
        <item name="android:paddingBottom">10dp</item>
        <item name="android:paddingLeft">10dp</item>
        <item name="android:paddingRight">10dp</item>
    </style>

</resources>
